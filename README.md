# gecko
A statically typed, compiled systems programming language with simplicity and intuitiveness in mind

```rust
// Hello World

use std::io;

#[entry]
proc main() -> int {
  return 2 * (32/8)**2;
}
```
